# Mail Service Configuration
# Available options: mailhog, gmail, smtp
MAIL_SERVICE=mailhog

# Primary SMTP Configuration
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Gmail Configuration (when MAIL_SERVICE=gmail)
EMAIL_USER=nodered@test.local
EMAIL_PASS=

# Fallback MailHog Configuration
FALLBACK_SMTP_HOST=localhost
FALLBACK_SMTP_PORT=1025
FALLBACK_SMTP_USER=
FALLBACK_SMTP_PASS=